{
  "name": "hidamari-assets",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "アバターのパーツを生成・最適化する道具立て（Blender + glTF Transform）",
  "scripts": {
    "build": "blender --background --python avatar.blend.py -- --out build && npm run optimize",
    "optimize": "node optimize.mjs build ../client/assets"
  },
  "devDependencies": {
    "@gltf-transform/core": "^4.2.1",
    "@gltf-transform/extensions": "^4.2.1",
    "@gltf-transform/functions": "^4.2.1"
  }
}
